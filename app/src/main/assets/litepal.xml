<?xml version="1.0" encoding="utf-8"?>
<litepal>
    <dbname value="BookStore"></dbname>
    <!--
    <dbname>标签指定数据库名
    <version>标签用于指定数据库版本号
    <list>标签用于指定所有的映射模型
    -->
    <version value="1"></version>

    <list>
        <mapping class = "com.example.dell.litepaltest.Book"></mapping>
        <mapping class="com.example.dell.litepaltest.Category"></mapping>
    </list>
    <!--
    <mapping>标签来声明我们要配置的映射模型类
    注意，一定要使用完整的类名
    不管有多少个模型类需要映射，都使用同样的方式配置在<list>标签下即可
    -->
</litepal>